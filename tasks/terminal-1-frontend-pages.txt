=== TERMINAL 1 — Frontend Page Refactoring ===
Project: E:\testtest\weather-health-ai

GOAL:
Refactor all 5 pages (Dashboard, Health, AIRecommendations, Chat, Reports) to use the new components instead of inline markup.

BEFORE YOU START:
Read these files to understand the available components:
- frontend/src/types/index.ts
- frontend/src/components/weather/ (WeatherCard.tsx, AQICard.tsx, UVCard.tsx, ForecastCard.tsx)
- frontend/src/components/health/ (HealthScore.tsx, RiskGauge.tsx, HealthInsight.tsx, RiskChart.tsx)
- frontend/src/components/ai/ (ChatBox.tsx, Message.tsx, RecommendationCard.tsx, AIInsightCard.tsx)
- frontend/src/components/reports/ (WeeklyReport.tsx, TrendChart.tsx, ReportTable.tsx)
- frontend/src/components/layout/ (Navbar.tsx, Footer.tsx)
- frontend/src/hooks/ (useWeather.ts, useHealth.ts, useChat.ts, useRecommendations.ts)
- frontend/src/context/ (UserContext.tsx, ThemeContext.tsx)
- frontend/src/services/ (weatherApi.ts, healthApi.ts, chatApi.ts, reportApi.ts)

Read existing pages to understand current state:
- frontend/src/pages/Dashboard.tsx
- frontend/src/pages/Health.tsx
- frontend/src/pages/AIRecommendations.tsx
- frontend/src/pages/Chat/index.tsx
- frontend/src/pages/Reports/index.tsx

Main read:
- frontend/src/App.tsx (to understand routing and layout structure)
- frontend/src/index.css (for CSS custom properties and class conventions)

WHAT TO DO:

1. Refactor Dashboard.tsx
   - Replace inline metrics grid with WeatherCard, AQICard, UVCard components
   - Replace inline 7-day forecast with ForecastCard
   - Use useWeather hook for data fetching (loading/error/data pattern)
   - Keep the AI Summary section but extract it if possible
   - Keep the 24-hour trend SVG chart as-is (it's a custom visualization)

2. Refactor Health.tsx
   - Replace inline gauge with HealthScore component
   - Replace inline health impacts grid with RiskGauge components
   - Replace inline AI insight section with HealthInsight
   - Replace inline risk trend chart with RiskChart
   - Use useHealth hook for data

3. Refactor AIRecommendations.tsx
   - Use RecommendationCard for each recommendation
   - Use AIInsightCard for summary insights
   - Use useRecommendations hook

4. Refactor Chat.tsx (in frontend/src/pages/Chat/index.tsx)
   - Use ChatBox component for the chat interface
   - Use Message component for each chat bubble
   - Use useChat hook for message state management

5. Refactor Reports.tsx (in frontend/src/pages/Reports/index.tsx)
   - Use WeeklyReport for the main report view
   - Use TrendChart for charts
   - Use ReportTable for tabular data

6. Update App.tsx
   - Wrap the app with UserProvider and ThemeProvider
   - Integrate Navbar and Footer
   - Make sure all imported components are properly referenced

7. Remove or update frontend/src/services/api.ts
   - Delete or deprecate the old unified api.ts since services are now split
   - Check that no remaining code imports from services/api.ts

RULES:
- Preserve the existing visual design (glass-morphism, dark theme, color scheme)
- Data from hooks should flow into components via props
- Handle loading, error, and empty states in every page
- Run `cd E:\testtest\weather-health-ai\frontend && npx tsc --noEmit` after each major change
- Fix all type errors before finishing

VERIFICATION:
All pages should render without errors, TypeScript should pass with 0 errors.
