=== TERMINAL 2 — Backend Integration & Testing ===
Project: E:\testtest\weather-health-ai

GOAL:
Get the backend fully working — test all endpoints, fix runtime bugs, ensure Typhoon AI and OpenWeatherMap integrations work properly.

BEFORE YOU START:
Read all backend source files to understand the architecture:
- backend/src/server.ts (entry point, middleware wiring)
- backend/src/routes/ (all 5 route files)
- backend/src/controllers/ (all 5 controller files)
- backend/src/middleware/ (errorHandler.ts, validateRequest.ts)
- backend/src/services/weather/ (weather.service.ts, aqi.service.ts, uv.service.ts)
- backend/src/services/health/ (risk.service.ts, score.service.ts, analysis.service.ts)
- backend/src/services/ai/ (typhoon.service.ts, summary.service.ts, recommendation.service.ts)
- backend/src/services/reports/report.service.ts
- backend/src/config/supabase.ts
- backend/.env (check current config values)
- backend/package.json (check available scripts and dependencies)

WHAT TO DO:

1. Fix missing dependencies
   - Check package.json has all needed deps
   - If not, run `cd E:\testtest\weather-health-ai\backend && npm install <missing-package>`

2. Start the backend and test endpoints
   - Run `cd E:\testtest\weather-health-ai\backend && npm run dev` (or `npx ts-node src/server.ts`)
   - Test each endpoint with curl or PowerShell Invoke-WebRequest:

   Test weather:
   ```
   curl http://localhost:3000/
   curl http://localhost:3000/api/weather?city=Bangkok
   ```

   Test health analysis:
   ```
   curl -X POST http://localhost:3000/api/health/analyze -H "Content-Type: application/json" -d '{"userId":1,"city":"Bangkok"}'
   ```

   Test recommendations:
   ```
   curl http://localhost:3000/api/recommendations?analysisId=1
   ```

   Test chat:
   ```
   curl -X POST http://localhost:3000/api/chat -H "Content-Type: application/json" -d '{"userId":1,"question":"What is the weather like today?"}'
   ```

   Test reports:
   ```
   curl http://localhost:3000/api/reports?userId=1
   ```

3. Fix issues found during testing:
   - Missing imports or wrong paths
   - TypeError: undefined is not an object (wrong property access)
   - Supabase queries failing (wrong table names, column names)
   - Typhoon API call failures (wrong URL, API key, request format)
   - CORS issues
   - Route handler not matching

4. Check error handling
   - Make sure errorHandler middleware catches all errors
   - Add try/catch in controllers where missing
   - Ensure proper HTTP status codes (200, 400, 404, 500)

5. Check the Typhoon AI service
   - Verify the API URL and format match OpenTyphoon's actual API
   - The env expects: TYPHOON_API_URL=https://api.opentyphoon.ai/v1/chat/completions
   - Format is OpenAI-compatible: { model, messages, max_tokens }

6. Ensure TypeScript compiles
   - Run `cd E:\testtest\weather-health-ai\backend && npx tsc --noEmit`
   - Fix any type errors

7. Run the server and keep it running
   - The backend should start without errors on port 3000

8. Verify the config/supabase.ts works
   - It should create a valid Supabase client
   - Handle missing env vars gracefully
   - If Supabase is not configured (placeholder keys), the app should still start but fail gracefully on DB calls

VERIFICATION:
Backend starts without errors, all endpoints return valid JSON responses.
